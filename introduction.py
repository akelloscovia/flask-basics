# What is a flask?
   # Flask is a light weight micro frame work used to  create web application ( wedsites, web systems,API)
# What are flask components?
    # Werkzeug (routing, debugging,wed servergateway interface(WSGI))
    # Jinja2 (templating engine(UI))
    # Markupsafe
    # Click
# Advantages of flasks
    #Light weight ie portable
    #Microframework
    #Enables easy routing
    #Flexibility
    #Provides an inbuilt debugger 
# What is an API?  